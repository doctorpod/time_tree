* When filter is used activities using & are ignored even if they would have matched
* Remove rspec depreciation warnings
* Improve code quality with rubocop
* Title to output. E.g. Date or query used
